<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hcmut.testiq">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.LoginActivity"
            android:theme="@style/AppTheme.LoginStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.RegisterActivity"
            android:theme="@style/AppTheme.LoginStyle" />
        <activity
            android:name=".activities.GetPasswordActivity"
            android:theme="@style/AppTheme.LoginStyle" />
        <activity
            android:name=".activities.MainMenuActivity"
            android:theme="@style/AppTheme.LoginStyle" />
        <activity
            android:name=".activities.StartTestActivity"
            android:theme="@style/AppTheme.LoginStyle" />
        <activity
            android:name=".activities.DetailTestActivity"
            android:theme="@style/AppTheme.LoginStyle" />
        <activity
            android:name=".activities.DiscussionMainActivity"
            android:theme="@style/AppTheme.LoginStyle" />
        <activity android:name=".activities.DiscussionDetailActivity"
            android:theme="@style/AppTheme.LoginStyle"></activity>
    </application>

</manifest>